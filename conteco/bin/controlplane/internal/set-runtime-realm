#!/usr/bin/env bash

export CONTECO_REALM_RUNTIME="$CONTECO_REALM"
realm="$2"
if [[ " reset continue " != *"$1"* ]] ; then
  realm="$1"
fi
if [[ "$realm" != "" ]] ; then
  echo "Amending CONTECO_REALM to $realm"
  echo ""
  export CONTECO_REALM_RUNTIME="$realm"
fi
